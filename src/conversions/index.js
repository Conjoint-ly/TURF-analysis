const { moreOrEqual } = require('./moreOrEqual');
const { more } = require('./more');
const { lessOrEqual } = require('./lessOrEqual');
const { less } = require('./less');
const { top } = require('./top');

module.exports = {
  less,
  lessOrEqual,
  more,
  moreOrEqual,
  top,
};
